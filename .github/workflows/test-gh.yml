name: test-gh
on:
  push:
    paths:
      - .github/workflows/test-gh.yml
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      GH_PAT: ${{ secrets.GH_PAT }}
    steps:
      - run: echo "Test for test-gh"
