name: Debug Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GH_PAT length: ${#GH_PAT}"
          python3 -c "import os; print('GH_PAT:', 'SET' if os.environ.get('GH_PAT') else 'NOT SET')"
