name: repo-doctor
on:
  push:
    paths:
      - .github/workflows/repo-doctor.yml
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      GH_PAT: ${{ secrets.GH_PAT }}
    steps:
      - run: echo "Test"
