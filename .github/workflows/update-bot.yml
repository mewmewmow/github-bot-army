name: update-bot
on:
  push:
    paths:
      - .github/workflows/update-bot.yml
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      GH_PAT: ${{ secrets.GH_PAT }}
    steps:
      - run: echo "Test"
